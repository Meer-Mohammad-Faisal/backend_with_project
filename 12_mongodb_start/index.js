/// what is cluster? copuple of server is called cluster.

// why cluster instead of server?(replica)

// what is mongodb atlas?
// what is mongodb compass?


// field?document?collection?


// const findResult = await collection.find({}).toArray(); // here .toArray make a network call (not collection.find())
    //console.log('Found documents =>', findResult); // teach me what is cursor? in this context


/// throug toArray all the data comes at one time, by this our system can't hancle as much of data at a time
// that's why we use for of loop(it work on one by one)

/// in simple word: toArray(1 saath); for of(1, 1 krke)
